# 切一半 — 产品需求文档（v0.4 MVP）

> 文档状态：MVP 阶段，纳入道具系统
> 版本说明：v0.4 更新 — 新增默认 3 刀机制 + 2 个 MVP 道具（撤销剑/再切一刀）+ 倒计时自动用道具
> 更新日期：2026-06-02
> 上一版本：v0.3（2026-06-02）

---

## 一、概述

**游戏名称：** 切一半
**游戏类型：** 休闲益智 / 精准操作
**一句话描述：** 玩家通过自由拖动切割线，将随机物体精准平分为两半，**精确切到 50% 即通关**。

**MVP 目标：** 用最少的开发资源验证核心玩法手感 — 拖动切割线 → 切到 50% → 通关的闭环是否好玩。

**核心设计权衡：** 判定采用 **50% 严格命中**（无任何误差容许），用 **MVP 阶段无限重试**（详见 2.4 节）来给玩家容错空间。**次数容许替代误差容许**。

### MVP 范围说明

| ✅ MVP 包含（必做） | ❌ MVP 不包含（后续迭代） |
|-------------------|------------------------|
| 单一关卡场景 + 矩形物体 | 多种复杂形状（心形、镂空等） |
| 拖动切割线核心交互 | 多种关卡主题/皮肤 |
| 50% 严格判定（无误差容许） | 通关星级系统 |
| 通关/失败基础反馈 | 排行榜/无尽模式 |
| 主菜单 + 设置入口 | 主菜单深度功能（商店、分享等） |
| 失败后可重新开始 | 复杂道具系统 |
| 激励视频获刀（可选） | 插屏广告、Banner 广告 |

---

## 二、核心玩法

### 2.1 核心循环

```
关卡开始 → 展示物体 → 玩家拖动切割线 → 确认切割
    ↓
掉落部分掉落，显示实际切割比例
    ↓
切割比例 = 50%？ ──是──→ 通关
    │
    否
    ↓
游戏失败 → 重新开始
```

**掉落的部分直接消失，不参与后续计算。**

### 2.2 切割机制

| 要素 | 说明 |
|------|------|
| **操作方式** | 玩家在屏幕内**自由拖动一条水平切割线**（可上下移动，固定水平方向） |
| **切割线样式** | 发光刀光效果：亮白色 `#FFFFFF`，线条粗细 3-4px，中心高亮 1px，**物体边缘叠加 1px 半透明黑色描边**（防止白线在浅色物体上不可见） |
| **切割线范围** | 钳制在物体上下边界内（不可拖出物体） |
| **拖动响应** | 1:1 实时跟随手指，**无阻尼/无惯性**；**吸附到整数像素**（避免浮点误差） |
| **可切区提示** | 物体顶部/底部边缘有微高亮（0.3 alpha），暗示切割范围 |
| **切割确认** | 拖动到位后点击"切割"按钮确认（手势释放直接切放到 M3 体验优化） |
| **切割动画** | 刀光划过（0.2s）→ 物体从切割线处分离 → 下方部分受重力掉落（0.5s） |
| **结果展示** | 掉落瞬间在屏幕中央弹出大字：**"XX%"**（实际切割比例，48-72px 大字号） |
| **判定时机** | 掉落动画结束后开始判定 |

### 2.3 胜负判定

```
切割比例 = 切割线位置 / 物体总高度 × 100%

判定规则（严格比较，无任何误差容许）：
- 切割比例 严格等于 50% → 通关
- 切割比例 ≠ 50% → 失败
```

**核心说明：**
- 切得越靠**下**，上面留下的越多；切得越靠**上**，掉下去越多。
- **判定不设任何容许范围**，必须严格命中 50% 才能通关 — 这是一款"差 0.1% 也是失败"的精准操作游戏。
- **容错由次数承担，不由误差承担**：MVP 阶段玩家可无限重试（详见 2.4 节），靠反复尝试命中 50%。
- **设计动机：** 严苛判定（差 0.1% 也算输）是核心乐趣来源 — 通关那一刻的爽感来自"几乎不可能命中"的胜利，靠玩家反复尝试达成。
- **技术实现：** 切割线 Y 坐标和物体高度都**量化到整数像素**，切割比例 = 整数/整数，避免浮点比较导致永远不成立。

### 2.4 刀数管理（v0.4 新增）

> **MVP 修订：** v0.4 起，每关卡固定 3 刀，失败消耗 1 刀，3 刀用完且无道具可用 = 游戏失败。

| 场景 | 刀数变化 | 结果 |
|------|----------|------|
| 进入关卡 | 3 刀 | 开始游戏 |
| 切到 50% | 0 刀消耗 | **通关**（无论剩几刀）|
| 切错（非 50%） | -1 刀 | 进入失败弹窗，可选道具或重开 |
| 3 刀用完 | 0 刀 | **游戏失败**（Game Over 画面）|
| 撤销剑 | 0 刀消耗 | 恢复上次切割线位置 + 重置刀数到用前状态 |
| 再切一刀 | 0 刀消耗 | 看完广告 + 1 刀 + 直接回到游戏 |

**核心规则**：
- 切到 50% 立即通关，不消耗刀数
- 失败消耗 1 刀，但**有 3 秒倒计时**让玩家决定是否用道具
- 倒计时结束自动处理（详见 2.5）

---

## 三、道具系统（v0.4 新增）

> **MVP 修订：** v0.4 纳入 2 个 MVP 道具，剩余 4 个道具推到 M4 跟进。详细设计见 `docs/ITEMS-DESIGN.md`。

### 3.1 MVP 道具

| 道具 | 图标 | 效果 | 获取方式 | 触发时机 |
|------|------|------|---------|----------|
| **撤销剑** | ↩️ | 恢复上次切割线位置（不消耗刀） | 激励视频 | 失败弹窗内 |
| **再切一刀** | 🔪 | 看完广告 + 1 刀 + 直接回到游戏 | 激励视频 | 失败弹窗内 |

### 3.2 倒计时机制（v0.4 新增）

失败后弹窗出现 → **3 秒倒计时**开始：

```
[失败画面] → [3 秒倒计时]
              ↓
       倒计时中：玩家可点道具
              ↓
       倒计时 0：
         ├─ 有撤销剑 → 自动使用
         ├─ 有再切一刀 → 弹"看广告"按钮
         └─ 无道具 → 弹"游戏失败"画面
```

### 3.3 M4 阶段（v0.5 跟进）

- 减半（差距减半）
- 放大镜
- 中心线
- 自动切（彩蛋）

完整设计见 `docs/ITEMS-DESIGN.md`。

---

## 四、难度设计

### 4.1 难度维度

> **MVP 简化：** MVP 阶段只做矩形物体，所有关卡难度一致（统一从矩形开始验证手感）。

| 维度 | 说明 | MVP 状态 |
|------|------|---------|
| **形状复杂度** | 矩形 → 不规则多边形 → 凹形/空洞 | ❌ MVP 只做矩形 |
| **背景干扰** | 动态干扰元素 | ❌ MVP 无干扰 |
| **刀数限制** | 初始刀数随关卡递减 | ❌ MVP 无刀数限制 |
| **形状对称性** | 不对称形状无法目测找中心 | ❌ MVP 只做对称矩形 |

### 4.2 形状分类（后续迭代规划）

```
第一季目标：20 个关卡（后续迭代）

Lv 1-5   基础形状（矩形、正方形、圆形、三角形、梯形）
Lv 6-10  进阶形状（五边形、六边形、心形、月牙形）
Lv 11-15 复杂形状（哑铃形、葫芦形、凹槽形）
Lv 16-20 变态形状（回旋镖形、镰刀形、镂空形）

**MVP 阶段：** 只实现矩形，验证核心切割手感。
```

### 4.3 关卡结构

```
每个关卡 = 固定形状 + 初始刀数 + 目标精度要求
通关后解锁下一关
全关卡通关后开启无尽模式（随机形状，刀数无限）
```

**MVP 阶段：** 单一矩形关卡，无解锁/无尽模式。后续迭代再加入多关卡。

---

## 五、美术风格

### 5.1 整体风格：卡通风格

- 圆润的线条，柔和的颜色，无尖锐元素
- 物体：日常生活中可切割的物品（水果/食物/道具）
- UI：简洁卡通，有弹性动画反馈

### 5.2 配色方案（初版）

| 元素 | 颜色 |
|------|------|
| 主色调 | 橙色 `#FF7F50` |
| 辅助色 | 薄荷绿 `#98FB98` |
| 强调色 | 金黄 `#FFD700` |
| 背景色 | 米白 `#FFF8F0` |
| 切割线色 | 亮白（发光效果）`#FFFFFF` |

### 5.3 兼容设计

为支持后续风格切换：
- 所有美术资源通过 **配置表/JSON** 管理形状数据
- 主题色通过全局 CSS 变量定义
- 形状绘制逻辑与渲染层分离（SVG/Canvas 支持换肤）

---

## 六、游戏流程

### 6.1 整体流程

```
启动闪屏 → 主菜单 → 游戏场景 → 通关/失败
                ↓
            设置
                                        ↓
                                    重新切割
```

### 6.2 主菜单

> **MVP 简化：** MVP 阶段只保留核心入口。

- 游戏标题（带动画）
- 「开始游戏」按钮（直接进入矩形关卡）
- 「设置」按钮（音效/音乐开关）

### 6.3 游戏场景 UI 布局

```
┌─────────────────────────────┐
│  [返回]                     │  ← 顶部栏
│                             │
│      ┌───────────────┐      │
│      │               │      │
│      │   切割物体    │      │  ← 主游戏区
│      │   ←切割线→   │      │
│      │               │      │
│      └───────────────┘      │
│                             │
│                             │
│        [  切割  ]           │  ← 底部：切割按钮
└─────────────────────────────┘
```

**MVP 简化：** 顶部栏只显示关卡号和返回按钮，无广告入口；底部只有切割按钮，无刀数显示。

### 6.4 通关结算

**通关画面：**
- 大字显示「50%」+ 庆祝文案「完美！」
- 显示剩余刀数（如 "剩 1 刀通关！"）
- 「再来一局」按钮（MVP 阶段无下一关）

**失败画面（含情绪化文案 + 道具栏）：**

| 实际比例 | 显示文案 | 视觉色调 |
|---------|---------|---------|
| 49-51% | "差 X%！就差一点了～" | 黄色 |
| 45-49% 或 51-55% | "差 X%，再来！" | 橙色 |
| 40-45% 或 55-60% | "差 X%，再仔细看看" | 红色 |
| <40% 或 >60% | "差太远啦，物体中心在哪？" | 深红 |

**失败画面布局（v0.4 新增道具栏）：**

```
┌─────────────────────────────┐
│         47%                 │  ← 实际比例（情绪色）
│   差 3%，再来！             │  ← 文案
│                             │
│   切到 50% 才算赢            │
│   剩余刀数：2/3              │  ← v0.4 新增
│                             │
│   ┌──┐  ┌──┐  ┌──┐         │  ← 道具栏
│   │↩️│  │🔪│  │⏰│         │     ↩️ 撤销
│   └──┘  └──┘  └──┘         │     🔪 再切
│   3 秒后自动撤销            │     ⏰ 倒计时
│                             │
│   [↻ 重新开始]               │
│   [返回菜单]                │
└─────────────────────────────┘
```

**3 秒倒计时逻辑**：
- 进入失败画面 → 倒计时开始
- 倒计时中：玩家可点 ↩️ 或 🔪
- 倒计时 0：
  - 有撤销剑（拥有）→ **自动使用**（切割线回退）
  - 无撤销剑但有再切一刀 → 弹"看广告"按钮
  - 都没有 → 进入"游戏失败"画面

**游戏失败画面**（3 刀用完 + 无道具）：

| 实际比例 | 显示文案 |
|---------|---------|
| 49-51% | "就差一点点，下次一定能切到！" |
| 40-49% / 51-60% | "差一点了，再来！" |
| <40% / >60% | "加油，多练练手感" |

显示：
- 大字"游戏失败"
- 实际比例
- 剩余刀数（0）
- 道具栏（全部灰色/不可用）
- 「看广告 + 再来一局」按钮
- 「返回菜单」按钮

### 6.5 首次进入引导（MVP 包含）

**目的：** 防止新手不知道操作方式直接流失。

**引导流程：**
1. 玩家进入关卡后，**物体上闪一个"切到中间试试～"的提示箭头**（3 秒后自动消失）
2. 切割线初始位置在物体底部（默认会失败的极端位置）
3. 玩家**第一次拖动**时，提示箭头变为"拖动到这里试试"
4. 玩家**完成一次切割**（无论成败）后，引导永久关闭

> **MVP 简化：** 引导只在首次进入关卡时出现 1 次，后续重玩不再打扰。

---

## 七、变现设计

> **MVP 简化：** MVP 阶段**不接入任何广告**，专注核心玩法验证。变现机制后续迭代加入。

| 广告类型 | 触发场景 | 频率 | MVP 状态 |
|---------|---------|------|---------|
| **激励视频** | 获刀、获道具 | 后续迭代考虑 | ❌ MVP 不接入 |
| **插屏广告** | 通关后 | 后续迭代考虑 | ❌ MVP 不接入 |
| **Banner** | 主菜单（M4 加关卡选择后，此位置也会放） | 后续迭代考虑 | ❌ MVP 不接入 |

**后续迭代计划：**
- 第一批广告位：失败弹窗的激励视频（看广告获得额外一刀）
- 接入平台：字节小程序广告 SDK（`tt.miniGame` 环境）

---

## 八、技术方案

### 8.1 引擎选择

**Cocos Creator 3.8 LTS**（已安装对应 Skill）

### 8.2 关键模块

> **MVP 简化：** MVP 阶段只实现最核心的模块，其他模块后续迭代加入。

| 模块 | 实现方式 | MVP 状态 |
|------|---------|---------|
| 形状绘制 | Cocos UI + 矩形 Sprite | ✅ MVP 实现 |
| 切割线拖动 | Cocos Touch/Click 事件 + UI 节点 | ✅ MVP 实现 |
| 比例计算 | 切割线 Y / 物体总高度 | ✅ MVP 实现 |
| 切割动画 | 简单 tween（上下分离 + 下落） | ✅ MVP 实现 |
| 胜负判定 | 切割比例 严格 === 50%（切割线 Y 与物体高度都量化到整数像素，避免浮点误差导致永远不成立）| ✅ MVP 实现 |
| 音效 | 切割音效 + 通关音效 | ⚪ 可选（MVP 可暂缓） |
| 物理掉落 | Cocos 内置 2D 物理或自定义 tween | ⚪ MVP 用 tween 即可 |
| 数据配置 | JSON 关卡配置表 | ⚪ MVP 写死数据 |
| 广告接入 | `tt` API | ❌ MVP 不接入 |
| 复杂形状 | Mesh/SVG 导入 | ❌ MVP 不实现 |
| 射线检测 | Raycast | ❌ MVP 不需要 |

### 8.3 性能目标

> **MVP 简化：** MVP 阶段性能目标以可玩为准，无需极致优化。

- 首包 ≤ 2MB（抖音小游戏主包限制；配分包时主包上限 4MB + 单分包 ≤ 20MB）
- 60 FPS（iOS 高性能模式）
- 内存峰值 ≤ 300MB（普通游戏合理范围）

### 8.4 项目结构（MVP 初定）

> **MVP 简化：** MVP 阶段目录结构保持精简，专注核心代码。

```
assets/
├── scenes/                  # 游戏场景
│   ├── Menu.scene           # 主菜单
│   └── Game.scene           # 游戏场景
├── scripts/                 # TypeScript 组件
│   ├── GameManager.ts       # 游戏主逻辑（关卡、判定）
│   ├── CutLine.ts           # 切割线拖动组件
│   ├── UIManager.ts         # 结算界面、提示
│   └── AudioManager.ts      # 音效（可选）
├── prefabs/                 # UI 预制体
└── textures/                # UI 纹理、矩形物体贴图
```

**MVP 阶段无需：** resources/shapes、resources/audio、复杂 JSON 配置。

---

## 九、里程碑计划

### MVP 阶段（当前重点）

| 阶段 | 目标 | 产出 |
|------|------|------|
| **M1 原型** | 验证核心切割手感 | 矩形 + 切割线 + 50% 判定，3 个内可玩 Demo |
| **M2 MVP 内测** | 跑通完整闭环 | 矩形关卡 + 主菜单 + 设置 + 重新开始 |
| **M3 MVP 体验** | 打磨核心体验 | 增加音效 + 切割动画 + 失败反馈优化 |

**MVP 验收标准（v0.4 更新）：**

#### 基础流程
- ✅ 玩家可从主菜单进入关卡
- ✅ 默认 3 刀，关卡开始时显示 "X/3" 刀数
- ✅ 拖动切割线，实时看到切割线位置
- ✅ 点击"切割"按钮，物体分离 + 下落
- ✅ 切割比例 严格等于 50% → 通关画面，显示「完美！」+ 剩余刀数
- ✅ 切割比例 ≠ 50% → 失败画面 + 消耗 1 刀 + 显示道具栏
- ✅ 3 刀用完 + 无道具 → 游戏失败画面

#### 道具系统（v0.4 新增）
- ✅ 失败画面显示道具栏（撤销/再切/倒计时）
- ✅ 撤销剑：点击 → 切割线回退 + 刀数不消耗
- ✅ 再切一刀：点击 → 弹激励视频 → 看完 +1 刀 + 回到游戏
- ✅ 3 秒倒计时：倒计时 0 时自动用撤销剑（如果拥有）
- ✅ 倒计时 0 + 无道具 → 弹"游戏失败"画面
- ✅ 道具不跨关卡（MVP 简化）

#### 交互细节
- ✅ 切割线钳制在物体上下边界内，不可拖出
- ✅ 切割线 1:1 跟随手指，无阻尼/惯性
- ✅ 切割线 Y 坐标吸附到整数像素
- ✅ 切割线在浅色物体上叠加半透明黑色描边，可见

#### 引导
- ✅ 首次进入关卡，显示"切到中间试试"提示箭头（3秒后消失）
- ✅ 首次拖动时切换为"拖动到这里试试"
- ✅ 第一次切割完成后引导关闭

#### 反馈
- ✅ 通关显示「完美！」+ 50% 大字
- ✅ 失败按 4 个差距区间显示不同文案和色调
- ✅ 结果展示在屏幕中央（48-72px 大字）

#### 系统
- ✅ 设置可开关音效
- ✅ 60 FPS（iOS 高性能模式）
- ✅ 主包 ≤ 2MB

### 后续迭代规划（MVP 之后）

| 阶段 | 目标 | 产出 |
|------|------|------|
| **M4 内容扩展** | 增加关卡和形状 | 20 个关卡（矩形 → 复杂形状） |
| **M5 商业化** | 接入广告 | 激励视频获刀 + 插屏 + Banner |
| **M6 上线** | 发布抖音小游戏 | 提交审核 + 调优 |

---

## 十、开放问题

- [x] ~~切割误差容许范围是否合理？~~ → 已确定：判定时无误差容许，只有精确切到 50% 才能通关
- [x] ~~通关星级如何定义？是否影响后续关卡解锁？~~ → 已确定：无星级设计，失败时结算画面会显示实际切割比例和距离 50% 的差距
- [x] ~~广告冷却时间 60 秒是否合适？~~ → 已确定：MVP 阶段不接入广告，冷却时间后续迭代再考虑
- [x] ~~道具系统是否有其他创意想法？~~ → 已确定：MVP 阶段不实现道具系统，后续迭代规划 2 个道具
- [ ] 无尽模式是否做？做成什么样？（后续迭代）
- [ ] 是否需要排行榜？（后续迭代）
- [ ] MVP 阶段技术方案是否需要进一步细化？
- [ ] 矩形物体的视觉风格（纯色/边框/发光）？

---

*讨论记录请直接在此文档中补充，版本更新时注明日期和作者。*
